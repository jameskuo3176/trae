services:
  django:
    build:
      # docker-compose.yml lives in QoR_Recorder; the parent is required so
      # Docker can copy both QoR_Recorder and canonical frontend-vue sources.
      context: ..
      dockerfile: QoR_Recorder/Dockerfile
      target: django-runtime
    image: qor-recorder-django:${IMAGE_TAG:-latest}
    container_name: qor-recorder-django
    environment:
      HOST: 0.0.0.0
      PORT: 8000
      DEBUG: ${DEBUG:-0}
      SECRET_KEY: ${SECRET_KEY:?Set SECRET_KEY in QoR_Recorder/.env}
      ENFORCE_SECRET_KEY: ${ENFORCE_SECRET_KEY:-1}
      ALLOWED_HOSTS: ${ALLOWED_HOSTS:-localhost,127.0.0.1}
      CSRF_TRUSTED_ORIGINS: ${CSRF_TRUSTED_ORIGINS:-}
      SESSION_COOKIE_SECURE: ${SESSION_COOKIE_SECURE:-0}
      SECURE_SSL_REDIRECT: ${SECURE_SSL_REDIRECT:-0}
      SECURE_HSTS_SECONDS: ${SECURE_HSTS_SECONDS:-0}
      SECURE_HSTS_INCLUDE_SUBDOMAINS: ${SECURE_HSTS_INCLUDE_SUBDOMAINS:-0}
      SECURE_HSTS_PRELOAD: ${SECURE_HSTS_PRELOAD:-0}
      # PostgreSQL stores Django metadata; Mongo stores heavy QoR documents.
      DB_TYPE: ${DB_TYPE:-sql}
      DATABASE_URL: ${DATABASE_URL:-}
      POSTGRES_DB: ${POSTGRES_DB:-qor_recorder}
      POSTGRES_USER: ${POSTGRES_USER:-qor}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD in QoR_Recorder/.env}
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      PERSISTENCE_MODE: ${PERSISTENCE_MODE:-mongo}
      MONGODB_URI: ${MONGODB_URI:-mongodb://mongo:27017}
      MONGODB_DB: ${MONGODB_DB:-qor_recorder}
      MONGODB_TIMEOUT_MS: ${MONGODB_TIMEOUT_MS:-2000}
      DATA_DIR: /app/data
      UPLOAD_FOLDER: /app/uploads
      BACKUP_DIR: /app/backups
      FRONTEND_MODE: ${FRONTEND_MODE:-vue}
      FRONTEND_URL: ${FRONTEND_URL:-/}
      GUNICORN_WORKERS: ${GUNICORN_WORKERS:-3}
      GUNICORN_TIMEOUT: ${GUNICORN_TIMEOUT:-120}
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
      - ./backups:/app/backups
    depends_on:
      postgres:
        condition: service_healthy
      mongo:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/health', timeout=3)"
      interval: 30s
      timeout: 5s
      start_period: 20s
      retries: 3

  postgres:
    image: postgres:16-alpine
    container_name: qor-recorder-postgres
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-qor_recorder}
      POSTGRES_USER: ${POSTGRES_USER:-qor}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD in QoR_Recorder/.env}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-qor} -d ${POSTGRES_DB:-qor_recorder}"]
      interval: 10s
      timeout: 5s
      start_period: 10s
      retries: 10

  mongo:
    image: mongo:7.0
    container_name: qor-recorder-mongo
    command: ["mongod", "--bind_ip_all"]
    volumes:
      - ./mongodbdir:/data/db
    restart: unless-stopped
    healthcheck:
      test:
        - CMD
        - mongosh
        - --quiet
        - --eval
        - "quit(db.adminCommand('ping').ok ? 0 : 2)"
      interval: 15s
      timeout: 10s
      start_period: 20s
      retries: 10

  nginx:
    build:
      context: ..
      dockerfile: QoR_Recorder/Dockerfile
      target: frontend-runtime
    image: qor-recorder-web:${IMAGE_TAG:-latest}
    container_name: qor-recorder-nginx
    ports:
      - "${HTTP_PORT:-80}:80"
    depends_on:
      django:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "nginx", "-t"]
      interval: 30s
      timeout: 5s
      start_period: 10s
      retries: 3

volumes:
  postgres_data:
